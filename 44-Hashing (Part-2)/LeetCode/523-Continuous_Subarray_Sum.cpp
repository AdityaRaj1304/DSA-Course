#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    bool checkSubarraySum(vector<int>& nums, int k) {
        unordered_set<int>s;
        s.insert(0);
        int sum = nums[0];
        for(int i = 1 ;i<nums.size();i++){
            sum+=nums[i];
            if()
        }
    }
};